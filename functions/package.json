{
  "name": "functions",
  "description": "Cloud Functions do Financeiro App — infraestrutura de backend (Etapa 1: validação local via Emulator Suite, sem deploy)",
  "private": true,
  "main": "index.js",
  "engines": {
    "node": "20"
  },
  "scripts": {
    "serve": "firebase emulators:start --only auth,firestore,functions",
    "shell": "firebase functions:shell",
    "logs": "firebase functions:log",
    "test": "jest --runInBand"
  },
  "dependencies": {
    "firebase-admin": "^14.2.0",
    "firebase-functions": "^7.3.2"
  },
  "devDependencies": {
    "@babel/preset-env": "^7.29.7",
    "jest": "^30.4.2"
  }
}
